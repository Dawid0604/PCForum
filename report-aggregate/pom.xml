<project>
    <parent>
        <groupId>pl.dawid0604</groupId>
        <artifactId>pcforum-root</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>report-aggregate</artifactId>
    <name>PCForum - JaCoCo Report Aggregate</name>
    <packaging>pom</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
    	  		<version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>report-aggregate</id>

                        <goals>
                            <goal>report-aggregate</goal>
                        </goals>

                        <phase>verify</phase>
                    </execution>
                </executions>

                <configuration>
                    <excludes>
                        <exclude>**/PCForum**ServiceApplication.class</exclude>
                        <exclude>**/configuration/CloudinaryConfig.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <!-- CONFIG SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>config-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>config-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- DISCOVERY SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>discovery-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>discovery-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- USER SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>user-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>user-service-persistence</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>user-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>user-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>user-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>user-service-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- NOTIFICATION SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>notification-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>notification-service-persistence</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>notification-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>notification-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>notification-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- THREAD SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>thread-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>thread-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>thread-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>thread-service-persistence</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>thread-service-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>thread-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- POST SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>post-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>post-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>post-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>post-service-persistence</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>post-service-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>post-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- CATEGORY SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>category-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>category-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>category-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>category-service-persistence</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>category-service-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>category-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- IMAGE SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>image-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>image-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>image-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>image-service-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>image-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- MESSAGE SERVICE MODULES -->
        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>message-service-configuration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>message-service-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>message-service-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>message-service-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>message-service-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>pl.dawid0604</groupId>
            <artifactId>message-service-persistence</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
