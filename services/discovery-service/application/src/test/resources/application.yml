spring:
  application:
    name: discovery-service

  config:
    import: optional:configserver:http://${CONFIG_SERVICE_INSTANCE:localhost}:${CONFIG_SERVICE_PORT}

  output:
    ansi:
      enabled: ALWAYS